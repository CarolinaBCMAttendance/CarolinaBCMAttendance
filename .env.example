# Copy values into your shell or process manager when needed.
PORT=3000

# Comma-separated frontend origins allowed to call the API.
# Leave unset or use * to allow any origin (local/dev).
# CORS_ORIGIN=https://your-frontend.example.com
