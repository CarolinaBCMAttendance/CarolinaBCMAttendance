// Frontend runtime config for BCM attendance.
// When the website is hosted on a different server than the API, set apiBaseUrl
// to the full API base, for example:
//   "https://api.example.com/api/v1/bcm"
// Leave empty to call the same origin (default when using `npm start`).
export const BCM_CONFIG = {
  apiBaseUrl: "",
};
